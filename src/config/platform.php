<?php

use App\Models\User;

return [
    /*
    |--------------------------------------------------------------------------
    | Маршрутизация панели управления
    |--------------------------------------------------------------------------
    |
    | Настройки домена и префикса маршрутов Orchid, а также список middleware,
    | которые применяются к публичным и защищённым разделам панели.
    |
    */

    'domain' => env('ORCHID_DOMAIN'),

    'prefix' => env('ORCHID_PREFIX', 'admin'),

    'middleware' => [
        'public' => [
            'web',
        ],
        'private' => [
            'web',
            'platform',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | Аутентификация
    |--------------------------------------------------------------------------
    |
    | Конфигурация guard и провайдера пользователей для панели Orchid.
    |
    */

    'auth' => [
        'guards' => [
            'web' => [
                'driver' => 'session',
                'provider' => 'users',
            ],
        ],

        'providers' => [
            'users' => [
                'driver' => 'eloquent',
                'model' => User::class,
            ],
        ],

        'passwords' => [
            'users' => [
                'provider' => 'users',
                'table' => 'password_reset_tokens',
                'expire' => 60,
                'throttle' => 60,
            ],
        ],

        'password_timeout' => 10800,
    ],

    /*
    |--------------------------------------------------------------------------
    | Хранилище вложений
    |--------------------------------------------------------------------------
    |
    | Диск и путь хранения загруженных файлов панели управления.
    |
    */

    'uploads' => [
        'disk' => env('ORCHID_STORAGE_DISK', 'public'),
        'path' => env('ORCHID_STORAGE_PATH', 'platform'),
    ],
];
